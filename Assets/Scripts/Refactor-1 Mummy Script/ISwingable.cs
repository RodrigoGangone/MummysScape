using UnityEngine;

/// <summary>
/// ISwingable
/// Contrato para objetos a los que puedo hacer swing (balancearme).
/// </summary>
public interface ISwingable
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
